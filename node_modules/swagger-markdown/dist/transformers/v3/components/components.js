"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformComponents = void 0;
const markdown_1 = require("../../../lib/markdown");
const processSchemas_1 = require("./processSchemas");
function transformComponents(components) {
    let hasComponents = false;
    const md = markdown_1.Markdown.md();
    md
        .line(md.string('Schemas').h3())
        .line();
    Object.keys(components).forEach((componentName) => {
        if (componentName === 'schemas' && components.schemas) {
            const processedSchemas = (0, processSchemas_1.processSchemas)(components.schemas);
            hasComponents = true;
            md.line(processedSchemas);
        }
    });
    if (hasComponents) {
        return md.export();
    }
    return null;
}
exports.transformComponents = transformComponents;
