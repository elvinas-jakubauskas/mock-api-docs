"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformHTTPSecurityScheme = exports.isHttpSecurityScheme = void 0;
const markdown_1 = require("../../../lib/markdown");
function isHttpSecurityScheme(scheme) {
    return scheme.type === 'http';
}
exports.isHttpSecurityScheme = isHttpSecurityScheme;
function transformHTTPSecurityScheme(name, scheme) {
    const md = markdown_1.Markdown.md();
    md.line(md.string(`${name} (HTTP, ${scheme.scheme})`).h4());
    if (scheme.description) {
        md.line(md.string(scheme.description).br());
    }
    if (scheme.bearerFormat) {
        md.line(`Bearer format: ${scheme.bearerFormat}`);
    }
    return md.export();
}
exports.transformHTTPSecurityScheme = transformHTTPSecurityScheme;
