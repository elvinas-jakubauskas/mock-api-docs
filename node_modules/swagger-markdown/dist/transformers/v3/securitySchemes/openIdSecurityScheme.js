"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformOpenIdSecurityScheme = exports.isOpenIdSecurityScheme = void 0;
const markdown_1 = require("../../../lib/markdown");
function isOpenIdSecurityScheme(scheme) {
    return scheme.type === 'openIdConnect';
}
exports.isOpenIdSecurityScheme = isOpenIdSecurityScheme;
function transformOpenIdSecurityScheme(name, scheme) {
    const md = markdown_1.Markdown.md();
    md.line(md.string(`${name} (OpenID Connect)`).h4());
    if (scheme.description) {
        md.line(md.string(scheme.description).br());
    }
    if (scheme.openIdConnectUrl) {
        md.line(md.string(`OpenID Connect URL: ${scheme.openIdConnectUrl}`).br());
    }
    return md.export();
}
exports.transformOpenIdSecurityScheme = transformOpenIdSecurityScheme;
